# models folder

it contains files like:- metrics_model.py and loader.py file

it Defines structure of data.
it creates and manages LLM connection
Also avoid repeated inialization

in one line:-
Models define schema and validation for API responses.


